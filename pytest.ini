[pytest]
asyncio_mode = auto
# Fixtures share the scope of the test that uses them. The engine's pool is disposed per
# test (tests/conftest.py), so no connection outlives the loop that opened it.
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
